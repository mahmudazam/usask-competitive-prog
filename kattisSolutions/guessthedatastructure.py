from collections import deque
import heapq
import sys

def main():
    ins=[]
    for line in sys.stdin:
        ins.append(line.strip().split())
    ind=0
    it=0
    while ind<len(ins):
        if len(ins[ind])==1:
            it=int(ins[ind][0])
        else:
            for 
main()    
